import React from 'react';


const Arrow = (props) => {

  const styles = {
    width: "30px",
    height: "1px",
    backgroundColor: "white",
    margin: "0 10px"
  };

  return (
    <div style={styles}>
    </div>
  );
};

export default Arrow;
